#include "MusicManager.hpp"

MusicManager* mmngr;

uuid MusicManager::loadFromAFile(irr::io::path audio_file_path)
{
    return "";
}

uuid MusicManager::loadFromDefFile(irr::io::path audio_file_path)
{
    return uuid();
}
